language: python
python:
  - "3.5"
before_install: cd bucketbook\ app/FlaskProject
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: test1.py
